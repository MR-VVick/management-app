import React from 'react';
import PublicRoutes from './routes/PublicRoutes';

const App: React.FC = () => {
  return (
    <div style={{ height: '100%' }}>
      <PublicRoutes />
    </div>
  );
};

export default App;
